#!/usr/bin/env bash

# Add or remove as many lines as you need (each one will be whitelisted!)
declare -a HOSTNAMES+=("host-1.example.com")
declare -a HOSTNAMES+=("host-2.example.com")
declare -a HOSTNAMES+=("host-3.example.com")
declare -a HOSTNAMES+=("host-4.example.com")
